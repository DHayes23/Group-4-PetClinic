- name: Update all packages  
  hosts: 172.31.33.153
  become: true
  tasks:
    - name: updating packages
      command: sudo apt-get update -y

  # install docker 
    - name: Install Docker
      apt:
        name: docker
        state: latest
        version: latest

# - name : Pull Docker image backend
#   hosts: backend
#   become: true
#   tasks :
#     - name: pulling docker image artifact from pipeline
